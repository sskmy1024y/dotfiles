# Docker Compose environment variables for dotfiles testing

# Operating System to test (ubuntu or archlinux)
OS=ubuntu

# Installation method (remote or local)
INSTALL_METHOD=remote

# Run cleanup after tests (true or false)
RUN_CLEANUP=false